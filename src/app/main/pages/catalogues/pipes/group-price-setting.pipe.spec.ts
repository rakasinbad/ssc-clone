import { GroupPriceSettingPipe } from './group-price-setting.pipe';

describe('GroupPriceSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupPriceSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
