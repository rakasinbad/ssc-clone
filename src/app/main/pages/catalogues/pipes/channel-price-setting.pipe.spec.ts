import { ChannelPriceSettingPipe } from './channel-price-setting.pipe';

describe('ChannelPriceSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelPriceSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
