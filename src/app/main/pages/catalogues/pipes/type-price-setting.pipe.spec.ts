import { TypePriceSettingPipe } from './type-price-setting.pipe';

describe('TypePriceSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new TypePriceSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
