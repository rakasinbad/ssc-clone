import { ClusterPriceSettingPipe } from './cluster-price-setting.pipe';

describe('ClusterPriceSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new ClusterPriceSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
