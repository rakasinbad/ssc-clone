import * as JourneyPlanStoreActions from './journey-plan-store.actions';

describe('JourneyPlanStore', () => {
  it('should create an instance', () => {
    expect(new JourneyPlanStoreActions.LoadJourneyPlanStores()).toBeTruthy();
  });
});
