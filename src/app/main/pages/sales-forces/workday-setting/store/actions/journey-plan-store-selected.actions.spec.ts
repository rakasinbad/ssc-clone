import * as JourneyPlanStoreSelectedActions from './journey-plan-store-selected.actions';

describe('JourneyPlanStoreSelected', () => {
  it('should create an instance', () => {
    expect(new JourneyPlanStoreSelectedActions.LoadJourneyPlanStoreSelecteds()).toBeTruthy();
  });
});
