import { SelectSearchFilterPipe } from './select-search-filter.pipe';

describe('SelectSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
