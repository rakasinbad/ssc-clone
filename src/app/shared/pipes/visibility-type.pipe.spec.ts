import { VisibilityTypePipe } from './visibility-type.pipe';

describe('VisibilityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VisibilityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
