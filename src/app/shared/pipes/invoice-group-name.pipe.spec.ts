import { InvoiceGroupNamePipe } from './invoice-group-name.pipe';

describe('InvoiceGroupNamePipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceGroupNamePipe();
    expect(pipe).toBeTruthy();
  });
});
