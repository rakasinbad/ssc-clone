import { SegmentationViewPipe } from './segmentation-view.pipe';

describe('SegmentationViewPipe', () => {
  it('create an instance', () => {
    const pipe = new SegmentationViewPipe();
    expect(pipe).toBeTruthy();
  });
});
