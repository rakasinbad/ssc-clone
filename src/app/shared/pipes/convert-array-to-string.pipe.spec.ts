import { ConvertArrayToStringPipe } from './convert-array-to-string.pipe';

describe('ConvertArrayToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertArrayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
