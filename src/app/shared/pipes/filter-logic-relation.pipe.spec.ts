import { FilterLogicRelationPipe } from './filter-logic-relation.pipe';

describe('FilterLogicRelationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLogicRelationPipe();
    expect(pipe).toBeTruthy();
  });
});
